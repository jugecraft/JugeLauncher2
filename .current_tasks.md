# Tasks

1.  **Transparency Fix**
    -   [x] Disable `transparent` in `tauri.conf.json`.
    -   [x] Enforce background color in `index.css`.

2.  **Logo Update**
    -   [ ] Ask user to save logo as `src/assets/logo.png`.
    -   [ ] Update `Sidebar.tsx` to use `logo.png`.
    -   [ ] Update `LoginScreen.tsx` to use `logo.png`.
